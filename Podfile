# Uncomment the next line to define a global platform for your project
# platform :ios, '13.6'

install! 'cocoapods', :disable_input_output_paths => true

target 'Hippo Chat POC' do
  # This project should work without `user_framworks!`
  # use_frameworks! 

  # pod 'Hippo', :git => 'https://github.com/Jungle-Works/Hippo-iOS-SDK.git'

  # Pods for Hippo Chat POC
  pod 'Hippo',:modular_headers => true

end
